# secrets
.env

# local env
.venv/
__pycache__/

# runtime state (lives on the host's data dir, not in git)
signatures.json
clerk_state.json
bills.json
acts.json
roles.json
votes.json

# personal scratch
w.md
